#include <bits/stdc++.h>

using namespace std;

class Solution
{
  public:
    //! Problem 1: Determine how many ways can a child run up the stairs
    //! 		   using either 1, 2 or 3 stairs at a time
    int tripleStep(const int &n);
};

int Solution::tripleStep(const int &n)
{
	
}